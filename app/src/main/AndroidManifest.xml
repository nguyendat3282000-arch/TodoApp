<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ══════════════════════════════════════════════════════════════
         PERMISSIONS
         ══════════════════════════════════════════════════════════════ -->

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Exact Alarms (API 31+: SCHEDULE_EXACT_ALARM; API 33+: USE_EXACT_ALARM) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <!-- Wake device screen for alarm -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <!-- Full-screen intent / show over lock screen -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Vibration -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Push Notifications (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Receive boot completed to re-schedule alarms after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground service for alarm (API 34+: FOREGROUND_SERVICE_MEDIA_PLAYBACK or ALARM) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- ══════════════════════════════════════════════════════════════
         APPLICATION
         ══════════════════════════════════════════════════════════════ -->
    <application
        android:name=".TodoApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodoApp"
        android:windowSoftInputMode="adjustResize"
        tools:targetApi="31">

        <!-- ── Main / Launcher Activity ────────────────────────────── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Alarm Screen Activity (FullScreenIntent target) ─────── -->
        <!--
            Shown when an alarm fires. It must be exported so the system
            can launch it via a PendingIntent from a BroadcastReceiver.
            showWhenLocked + turnScreenOn allow display over the lock screen.
        -->
        <activity
            android:name=".ui.screens.alarm.AlarmActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:taskAffinity=""
            android:theme="@style/Theme.TodoApp" />

        <!-- ── Alarm BroadcastReceiver ──────────────────────────────── -->
        <!--
            Fired by AlarmManager when a task's due date/time is reached.
            It starts a foreground service and posts the FullScreenIntent.
        -->
        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false" />

        <!-- ── Boot Receiver (re-schedule alarms after reboot) ───────── -->
        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ── Alarm Foreground Service ──────────────────────────────── -->
        <!--
            Keeps the alarm sound / vibration running even when the user
            is not actively interacting with the alarm screen.
            foregroundServiceType="mediaPlayback" covers audio on API 34+.
        -->
        <service
            android:name=".alarm.AlarmService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="alarm" />
        </service>

        <!-- ── Dismiss Alarm BroadcastReceiver ───────────────────────── -->
        <!--
            Handles the "Dismiss" action from the notification and the
            alarm screen itself.
        -->
        <receiver
            android:name=".alarm.DismissAlarmReceiver"
            android:exported="false" />

        <!-- ── Glance App Widget ─────────────────────────────────────── -->
        <!--
            exported="true" is required for APPWIDGET_UPDATE (system broadcast).
            The custom ACTION_REFRESH_WIDGET is sent by the app itself via
            sendBroadcast(), so it also needs to be in the intent-filter.
        -->
        <receiver
            android:name=".widget.TodoWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.todoapp.ACTION_REFRESH_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/todo_widget_info" />
        </receiver>

        <!-- ── Daily Summary Notification Receiver ────────────────────── -->
        <!--
            Handles "Mark All Done" / "Mark Task Done" actions tapped from
            the daily summary notification. Not exported — actions are sent
            by the app itself only.
        -->
        <receiver
            android:name=".notification.DailySummaryReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.todoapp.ACTION_MARK_ALL_DONE" />
                <action android:name="com.example.todoapp.ACTION_MARK_TASK_DONE" />
            </intent-filter>
        </receiver>

        <!-- ── Morning Motivation Notification Receiver ────────────────── -->
        <receiver
            android:name=".notification.MorningNotificationReceiver"
            android:exported="false" />

    </application>

</manifest>
